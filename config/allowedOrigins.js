const allowedOrigins = [
    // 'http://localhost:3000',
    'https://www.technotes.onrender.com',
    'http://dandrepairshop.com'
]

module.exports = allowedOrigins;



//cors is installed and imported in serverjs,
//it allows view our document from this registered ports apart from the normal default port
